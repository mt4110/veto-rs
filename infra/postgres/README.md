# Postgres (optional)

This is only needed if you later add audit-log storage or dashboards.

```bash
docker compose up -d
```

Connection example:
- user: `veto`
- password: `veto`
- db: `veto`
- host: `localhost:5432`
