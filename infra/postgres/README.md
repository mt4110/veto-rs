# Postgres (optional)

This is only needed if you later add audit-log storage or dashboards.

```bash
docker compose up -d
```

Connection example:
- user: `veri`
- password: `veri`
- db: `veri`
- host: `localhost:5432`
