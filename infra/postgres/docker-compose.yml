services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: veri
      POSTGRES_PASSWORD: veri
      POSTGRES_DB: veri
    ports:
      - "5432:5432"
    volumes:
      - veri_pgdata:/var/lib/postgresql/data
volumes:
  veri_pgdata:
