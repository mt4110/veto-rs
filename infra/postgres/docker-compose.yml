services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: veto
      POSTGRES_PASSWORD: veto
      POSTGRES_DB: veto
    ports:
      - "5432:5432"
    volumes:
      - veto_pgdata:/var/lib/postgresql/data
volumes:
  veto_pgdata:
